# context system

Drop-in project memory for AI sessions: MCP server + vector index in one folder.

```sh
cp -r .context <your-repo>/     # then add .mcp.json, see .context/README.md
```

Docs: [.context/README.md](.context/README.md)
